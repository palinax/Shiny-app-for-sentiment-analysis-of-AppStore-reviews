# library(shiny)
#
# ui <- fluidPage(
#   sidebarLayout(
#     sidebarPanel = sidebarPanel(
#       selectInput(inputId = "app_name", label = "App name", choices = 1:3)
#     ),
#     mainPanel = mainPanel(
#
#     )
#   )
# )
#
# server <- function(input, output, session) {
#
# }
#
# shinyApp(ui, server)
